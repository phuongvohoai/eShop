Run bellow command to set parameter for app:

```
dotnet user-secrets init
dotnet user-secrets set Parameters:postgres-password Password1@
dotnet user-secrets set Parameters:keycloak-password Password1@
```